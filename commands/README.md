# commands/

Custom slash commands live here. The filename (minus `.md`) is the command name.

So `commands/foo.md` becomes `/foo`.

See [`examples/commands/`](../examples/commands/) for working examples.
